"""
KoBERT 분류기 학습 스크립트
"""
import json
from kobert_classifier import train_kobert_classifier, TableTextClassifier


def load_training_data_from_json(json_path):
    """JSON 파일에서 학습 데이터 로드"""
    with open(json_path, 'r', encoding='utf-8') as f:
        data = json.load(f)

    train_data = []
    for item in data['train_data']:
        text = item['text']
        label = item['label']
        train_data.append((text, label))

    print(f"✅ 학습 데이터 로드: {len(train_data)}개")
    return train_data


def create_sample_data():
    """샘플 학습 데이터 생성 (최소 예시)"""
    train_data = [
        # 0: 제목
        ("표 3.2 연간 실적", 0),
        ("표 B.8 월별 기온", 0),
        ("표 4-21 예산 집행 현황", 0),
        ("□ 추진조직 구성", 0),
        ("■ 사업개요", 0),
        ("ㅇ 주요 추진 내용", 0),
        ("① 기본 방향", 0),
        ("② 세부 계획", 0),
        ("(1) 사업 추진 방향", 0),
        ("1) 추진 배경", 0),
        ("3.2 토지이용현황", 0),
        ("4.1.2 조사 방법", 0),
        ("표 A.6 환경영향평가", 0),
        ("표 5.1 재무 현황", 0),
        ("◆ 추진 전략", 0),
        ("③ 세부 실행계획", 0),
        ("2) 주요 내용", 0),
        ("표 C.12 사업비 내역", 0),
        ("5.3 운영 방안", 0),
        ("□ 기대 효과", 0),

        # 1: 설명
        ("상세한 내용은 다음 표 B.12와 같다", 1),
        ("표 A.20에 의하면 다음과 같은 결과를 얻었다", 1),
        ("표 3.2에서 나타난 바와 같이 지속적으로 증가하는 추세를 보인다", 1),
        ("※ 본 자료는 참고용입니다", 1),
        ("※ 상기 내용은 변경될 수 있습니다", 1),
        ("본 표는 2023년 기준으로 작성되었으며, 향후 변동이 있을 수 있습니다", 1),
        ("아래 표에서 보는 바와 같이 전년 대비 증가하였다", 1),
        ("다음 표 5.1에 따르면 매출이 증가했다", 1),
        ("표 B.4에서와 같이 분기별 실적을 나타낸다", 1),
        ("※ 2024년 1월 기준", 1),
        ("상기 표에 나타난 수치는 잠정치입니다", 1),
        ("아래 표는 최근 3년간의 추이를 보여준다", 1),
        ("본 자료는 내부 검토용으로 외부 공개를 금합니다", 1),
        ("다음 표 A.8을 참조하시기 바랍니다", 1),

        # 2: 제목아님
        ("123", 2),
        ("45", 2),
        ("© 2024 All Rights Reserved", 2),
        ("단위: ℃", 2),
        ("단위: %", 2),
        ("[단위 : mm]", 2),
        ("(단위: 백만원)", 2),
        ("-", 2),
        ("...", 2),
        ("1", 2),
        ("99", 2),
        ("unit: kg", 2),
        ("℃", 2),
        ("%)", 2),
        ("Copyright 2024", 2),
        ("---", 2),
        ("·", 2),
        ("※", 2),
    ]

    return train_data


def main():
    """학습 메인 함수"""
    print("=" * 60)
    print("KoBERT 표 텍스트 분류기 학습")
    print("=" * 60)

    # 옵션 1: JSON 파일에서 로드
    train_data = load_training_data_from_json('train_data_example.json')

    # 옵션 2: 샘플 데이터 사용 (주석 처리)
    # train_data = create_sample_data()

    print(f"\n📊 학습 데이터 통계:")
    labels_count = {0: 0, 1: 0, 2: 0}
    for _, label in train_data:
        labels_count[label] += 1

    print(f"  제목: {labels_count[0]}개")
    print(f"  설명: {labels_count[1]}개")
    print(f"  제목아님: {labels_count[2]}개")
    print(f"  총합: {len(train_data)}개")

    # 학습 파라미터
    epochs = 5  # 에폭 수 (데이터 적으면 늘리기)
    batch_size = 8  # 배치 크기 (GPU 메모리에 따라 조절)
    lr = 2e-5  # 학습률
    save_path = 'kobert_table_classifier.pt'

    print(f"\n⚙️  학습 파라미터:")
    print(f"  에폭: {epochs}")
    print(f"  배치 크기: {batch_size}")
    print(f"  학습률: {lr}")
    print(f"  저장 경로: {save_path}")

    # 학습 실행
    train_kobert_classifier(
        train_data=train_data,
        val_data=None,  # 검증 데이터 있으면 추가
        epochs=epochs,
        batch_size=batch_size,
        lr=lr,
        save_path=save_path
    )

    # 학습된 모델 테스트
    print("\n" + "=" * 60)
    print("학습된 모델 테스트")
    print("=" * 60)

    classifier = TableTextClassifier(model_path=save_path)

    test_cases = [
        "표 4.21 예산 집행 현황",
        "□ 추진 조직 구성",
        "ㅇ 사업 개요",
        "① 주요 내용",
        "상세한 내용은 다음 표와 같다",
        "표 A.20에 의하면 증가 추세를 보인다",
        "※ 본 자료는 참고용입니다",
        "123",
        "단위: ℃",
        "© 2024 All Rights Reserved",
    ]

    print("\n🧪 테스트 결과:")
    for text in test_cases:
        label, prob, all_probs = classifier.predict(text, return_probs=True)
        print(f"\n'{text}'")
        print(f"  → 예측: {label} (확신도: {prob:.3f})")
        print(f"  상세: 제목={all_probs[0]:.3f}, 설명={all_probs[1]:.3f}, 제목아님={all_probs[2]:.3f}")


if __name__ == '__main__':
    main()
